const { cat, dog, tiger} = { //비구조화 할당을 수행한 경우, "순서에 상관없이 키값에 맞는 값 대입"

	cat : "cat",
	dog : "dog",
	tiger2 : "tiger" //배열에 있는 키값과 다른 값
}; 

console.log(cat);
console.log(dog);
console.log(tiger);